# Code 1
def greet_students (name, nChar):
    for i in range(nChar):
        print(name[i])

name = input("Enter a Name: ")
nChar = input("Enter any numeric number: ")
nChar = int(nChar)
# modified code!
if nChar > len(name):
    print("Chosen number is greater than the length of the name. Please enter a valid number.")
else:
    greet_students (name, nChar)

# a. If name is “Joseph The Dreamer” and nChar is 5, what will be the output of the code above and why?
# - The output of the code will be "Josep" because the for loop iterates through the letters of the name variable from 0 to 4 (as nChar is 5).
# b. Using the same name and nChar is 20, what now is the output and why?
# - The output will be "Joseph The Dreamer" because the for loop will be able to iterate through all the letters of the name variable. However, since nChar is 20, it will cause an error message that indicates that the index is out of range.
# c. If there is an error message encountered in letter b, how will you be able to modify the code so that the error message will not appear.
# - I can modify the code by adding an if statement that checks if nChar is greater than the length of the name variable which can prevent the error.

# Code 2
def greet_students(name, nChar):
    for i in range(nChar):
        print(name[0 : nChar - i])

name = input("Enter a Name: ")
greet_students(name, len(name))

# a. Find the syntax error and modify it.  Please identify the error and what did you do to fix it?
# The syntax error in the initial code was the missing colon at the end of the for loop statement. To fix this error, I added a colon at the end of the for loop.

# Code 3
def sum_of_squared(n):
    sum = 0
    for i in range(1, n + 1):
        sum += pow(i, 2)
    return sum

n = 0
while n < 1 or n > 100:
    n = input("Enter a Number from 1 to 100 : ")
    n = int(n)

print("Sum of all squared numbers is", sum_of_squared(n))